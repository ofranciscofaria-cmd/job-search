# Job scan 2026-09-12

## Blocked: could not run this scan

The scan could not start. Every request to LinkedIn from this environment's network was rejected
before reaching LinkedIn at all: the outbound proxy's egress policy denies `www.linkedin.com:443`
with an HTTP 403 at the CONNECT stage (this is the sandbox's own network policy blocking the host,
not LinkedIn rate-limiting or bot-blocking the request).

Exact error (first attempt, `python3 li.py search "revenue operations" "Porto, Portugal" --days 3`):

```
OSError: Tunnel connection failed: 403 Forbidden
```

Confirmed via the proxy's own status endpoint (`$HTTPS_PROXY/__agentproxy/status`), which logged
three consecutive `connect_rejected` failures for `www.linkedin.com:443`, each `"gateway answered
403 to CONNECT (policy denial or upstream failure)"`. Per this environment's own guidance, a 403 at
this layer is an organization/network policy denial and should be reported rather than retried or
routed around, so no further searches were attempted this run.

No searches ran, so `seen-jobs.txt` was not modified.

## Notes (searches run, failures)
- 0 of the planned ~20 searches ran.
- Failure: outbound network access to linkedin.com is blocked by this session's egress proxy policy
  (403 on CONNECT), before any LinkedIn-side response was received.
- Action needed: this is an environment/network configuration issue, not something fixable by
  retrying the scan. Whoever manages this scheduled session's network policy needs to allow
  `www.linkedin.com` (and likely `www.linkedin.com:443` specifically) for this environment, or the
  nightly scan needs to run somewhere with an unblocked network path.
